= Module 1: Exploring the Platform
include::partial$_attributes.adoc[]
:imagesdir: ../assets/images

In this module you will log into OpenShift, explore the namespaces and applications that have been provisioned for you, and get familiar with the tools you will use throughout the lab.

== Log into OpenShift

. Open the *OpenShift Console* using the tab on the right side of this page, or navigate to {console_url}

. Click *rhsso* as the login provider

. Enter your credentials:
+
* *Username:* `{user}`
* *Password:* `{password}`

== Accessing your Projects (Namespaces)

. Navigate to *Home > Projects* in the *OpenShift Console*

. You should see three projects:
+
* `{build_ns}` — where your CI/CD pipelines run
* `{dev_ns}` — your development environment
* `{prod_ns}` — your production environment

Your list should resemble the following screenshot:

image:m1/user-namespaces.png[User namespaces as seen by a lab user]

Your access is governed by a custom `tenant-user` ClusterRole, i.e https://kubernetes.io/docs/reference/access-authn-authz/rbac/[Kubernetes RBAC]. This gives you admin-like permissions in your namespaces with one important restriction: you cannot view Secrets nor read their values. This is by design — secrets like database credentials, LLM API keys, and Kafka connection strings are managed and made available by the platform team through ExternalSecrets and stored HashiCorp Vault.

NOTE: You're using a set of user-specific namespaces created for this lab experience. In reality, an application like Parasol Insurance would have namespaces similar to `parasol-build`, `parasol-dev`, and `parasol-prod` that multiple developers can view.

== Explore the Dev Environment

. Click on the `{dev_ns}` project to open it

. Switch to the *Topology* view using the *Workloads > Topology* link on the left
+
image:m1/topology-dev.png[Topology view for the dev namespace]

. Click the small *route icon* (the arrow in the top-right corner of the *parasol-insurance* application circle) to open the Parasol Insurance application in a new tab

. In the Parasol application, navigate to the *Inbox* page
+
image:m1/parasol-inbox.png[The Parasol Insurance application's Inbox page]

. Observe the emails in the inbox. Notice that some emails are classified as *REVIEW REQUIRED*. This is because the current implementation uses simple keyword matching — any email the keywords cannot confidently classify gets flagged for manual review. You will fix this in Module 3.

Nice work so far! You've accessed the OpenShift Console, found the Topology view, and learned how to access an application's public endpoint. OpenShift provides a https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html/ingress_and_load_balancing/routes[Route resource] to define and manage public endpoints.

== Viewing Secrets (ExternalSecrets)

The Parasol Insurance application connects to Apache Kafka and consumes messages from a `email` Topic to perform routing to specific departments - this requires credentials to connect to the Kafka cluster. As mentioned earlier, the platform team at Parasol have standardized on HashiCorp Vault for secret management. The ExternalSecrets Operator is used to connect to the Vault instance and distribute those secrets to applications.

Let's view the ExternalSecrets:

. Navigate to *Home > Search*

. In the *Resources* dropdown, type `ExternalSecret` and select it

. Make sure you are viewing the `{dev_ns}` namespace

. You should see two ExternalSecrets:
+
* `litellm-credentials` — LLM API endpoint and key, synced from Vault
* `kafka-credentials` — Kafka connection details, synced from Vault

image:m1/external-secret-list-dev.png[The ExternalSecrets for Parasol Insurance's dev namespace]

These ExternalSecrets automatically create Kubernetes Secrets in your namespace by pulling values from HashiCorp Vault. The platform team configured this so that sensitive credentials never need to be committed to source code or manually managed.

Try clicking on one of the ExternalSecrets and note the *Status* showing it is synced. This means that the `kafka-credentials` Secret requested by the ExternalSecret of the same name will be available for the Parasol Insurance application to consume and use to connect to the Apache Kafka cluster.

NOTE: If you're wondering how these resources are created, how they're managed, and where they come from, that will be answered soon. Right now, we're just taking a tour and getting familiar with OpenShift and the application you'll work on during this lab.

== Explore the Build Environment

. Switch to the `{build_ns}` namespace using the project dropdown at the top

. Click *Pipelines > Pipelines* in the left sidebar. You should see `push` and `tag-promote` pipelines.
+
image:m1/pipelines-build.png[OpenShift Pipelines View]

. A `push` https://docs.redhat.com/en/documentation/red_hat_openshift_pipelines/1.21/html/about_openshift_pipelines/understanding-openshift-pipelines#about-pipelinerun_understanding-openshift-pipelines[PipelineRun] that has already completed — this was triggered automatically when your environment was provisioned to pre-build and pre-deploy the Parasol Insurance application. You viewed this in the `dev` environment earlier.

. Click on the link in the *Last run* column to view the PipelineRun to see its details. 
+
image:m1/pipeline-run-push.png[Completed Push PipelineRun]
. Examine pipeline stages, known as https://docs.redhat.com/en/documentation/red_hat_openshift_pipelines/1.21/html/about_openshift_pipelines/understanding-openshift-pipelines#about-tasks_understanding-openshift-pipelines[Tasks]:
+
* *clone* — clones your source code from GitLab
* *maven-build* — compiles the Quarkus application and runs tests
* *sonar-scan* — runs SonarQube static analysis for code quality
* *build-and-push* — builds the container image and pushes it to Quay
* *rollout-restart* — triggers a rolling restart of the deployment in the dev namespace

At this point you've observed that OpenShift provides multiple application-platform capabilities, including the ability to run CI/CD natively on the platform itself. 

As an optional exercise, return to the *Home > Search* page and view the ExternalSecrets defined in your *{build_ns}* namespace - you'll notice that multiple Secrets that are required to securely access code, push/pull images, and interact with SonarQube are defined. Platform adminstrators can rotate these
credentials in Vault at any time, and the ExternalSecrets will automatically synchronise and make sure the latest values are available to your application's and CI/CD pipelines.

== Your Source Code in GitLab

Finally! It's time to view application source code and manifests in GitLab.

. Open the *GitLab* tab on the right side of this page, or navigate to {gitlab_url}

. Log in with your credentials:
+
* *Username:* `{user}`
* *Password:* `{password}`

. After logging in, you should see two repositories forked into your *Projects* list:
+
* *parasol-insurance* — the Parasol Insurance application's Quarkus source code)
* *parasol-insurance-manifests* — the build and deployment manifests (Helm Charts) with Kubernetes CRs that are deployed and managed by OpenShift Gitops

Take a look at the manifests first. These define the build and deployment environments, using an Infrastructure as Code (IaC) approach:

. Click the *parasol-insurance-manifests* repository. A directory listing is shown.
. Open the `app/templates/` folder. Here you can see the ExternalSecrets (YAML files with `es-` prefix) and other CRs.
+
image:m1/gitlab-manifests.png[GitLab Parasol Manifests]
. Click the `deployment.yaml` and locate the `DEV_KAFKA_PASSWORD`. Notice that it references `kafka-credentials` defined by the ExternalSecret?

The manifests are deployed to your namespaces on OpenShift using OpenShift GitOps - you'll see this in-depth later.

Next, explore the Quarkus source code:

. Click the GitLab icon in the top-left of the GitLab UI to return to your *Projects* list.
. Click into the *parasol-insurance* repository and explore the project structure. This is a standard Quarkus application with `src/main/java` for source code and `src/main/resources` for configuration.
. Open `src/main/resources/application.properties` and take note of the `DEV_KAFKA_PASSWORD` environment variable. Recall that this is defined by the `deployment.yaml` https://kubernetes.io/docs/concepts/workloads/controllers/deployment/[Deployment] that reads the `es-kafka.yaml` ExternalSecret? At no point is the value of the credential stored in plaintext.
. Also, you'll notice `mp.messaging.incoming.emails-in.topic` in `application.properties` defines the Kafka Topic that the application consumes emails from.
+
image:m1/parasol-application-properties.png[Parasol Insurance application.properties file in GitLab]
. Take a look at the code in `src/main/java/com/parasol/EmailRouter.java` and you'll see the `consume` method references the `emails-in` message channel that was defined in `application.properties`.

Now you know how the application source code, and build and deployment manifests come together to form a complete application deployment on OpenShift. This application is designed with the https://12factor.net/[Twelve-Factor principles] in mind.

== Explore OpenShift GitOps

The manifests you explored in the {gitlab_url}/{username}/parasol-insurance-manifests[parasol-insurance-manifests] repository are used to manage all aspects of the application build and deployment on OpenShift via OpenShift GitOps (Argo CD). Essentially, OpenShift GitOps watches your manifest repository for changes, and continuously reconciles the state of the manifests on the cluster with those in the repository. This prevents configuration drift and ensures applications are in a known state.

=== View your Applications

Argo CD uses https://argo-cd.readthedocs.io/en/latest/user-guide/application-specification/[Application CRs] to manage manifests on your OpenShift cluster.

. Use the Argo CD tab, or visit the {argocd_url}[Argo CD UI] in a new tab.
+
IMPORTANT: Do not click the *Log in via OpenShift* button. 
. Enter your username and password to login: 
+
* *Username:* `{user}`
* *Password:* `{password}`
. You should see three Application tiles displayed. These represent your build, development, and production environments.
+ 
image:m1/argocd-apps.png[User Argo CD Application]

Enabling 

=== Update a Manifest in GitLab

These applications were created for you when your lab environment was provisioned. Each of these correspond to one of your three your namespaces. Let's see GitOps in action:

. Return to {gitlab_url}/{username}/parasol-insurance-manifests[parasol-insurance-manifests] in GitLab.
. Open the `app/templates/deployment.yaml` and select *Edit > Edit single file*.
. In the `metadata.labels` section, add the following label:
+
[source,text,role=execute,subs=attributes+]
----
app.openshift.io/runtime: quarkus
----
. Scroll down. Enter a commit message:
+
[source,text,role=execute,subs=attributes+]
----
fix: show the quarkus icon in the openshift topology view
----
. The result will be similar to the following image:
+
image:m1/gitlab-update-manifest.png[GitLab Deployment Manifest Update]
. Click *Commit changes*.

=== Synchronize the Application in Argo CD

. Return to the `{username}-dev` Application in Argo CD, and click *Refresh*.
+
NOTE: Clicking *Refresh* is not strictly required. Argo CD polls for changes every 3 minutes by default, and webhooks can be used to eliminate polling entirely.
. Once the refresh completes, you'll briefly see an *OutOfSync* message.
+
image:m1/argocd-out-of-sync.png[Argo CD OutOfSync]
. The *OutOfSync* status will quickly change to *Syncing* since auto-sync is enabled.
. Wait for the application to report *Synced*.

Return to the *Workloads > Topolgy* view in the OpenShift Console. Make sure the *{dev_ns}* Project is selected. You should see that the Quarkus icon is now displayed on the Parasol Insurance deployment.

image:m1/topology-dev-updated.png[Updated Topology View]

== What's Next

You have explored the platform: your namespaces, the running application, the pipeline, ExternalSecrets, and your source code. In the next module you will make a code change to Parasol Insurance using OpenShift Dev Spaces and an AI coding assistant, and watch it flow through the automated CI/CD pipeline.
